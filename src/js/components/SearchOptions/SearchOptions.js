export default function SearchOptions(recipes) {
    const divSearchOptions = document.createElement('div');

    divSearchOptions.textContent = `TODO Options Search`;
    divSearchOptions.classList.add('SearchOptions');

    return divSearchOptions;
}
